// Package engine owns the durable domain logic: the status state machine, merge/dedup, URL normalization, SimHash fingerprinting, trust scoring, and repost detection. It also runs Tier-1 declarative scraper manifests.
package engine
